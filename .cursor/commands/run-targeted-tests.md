# Run targeted tests

Use **`/tester`** instead — it writes tests with coverage requirements and runs pytest in parallel (`-n auto`).

This command is kept for backward compatibility. Open [`.cursor/commands/tester.md`](tester.md) or run `/tester`.
